# Design Documentation

Architecture diagrams and system documentation. Use Mermaid syntax (`.mmd` files).

Add diagrams here as needed (e.g. `system_context.mmd`, `data_model.mmd`). See `.cursor/rules/design-documentation-standards.mdc` for diagram types.
